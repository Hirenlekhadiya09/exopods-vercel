// import { configMenu } from "./menuConfig";

// export function getMenuPageByID(menu: keyof IConfigMenu, name: string): IMenuItem | undefined {
//     const pages = configMenu[menu];
//     const page = pages.find(item => item.id === name) as IMenuItem;

//     if (!page) {
//         throw new Error(`Page '${name}' not found in '${menu}' - configMenu`);
//     }

//     return page as IMenuItem;
// }
